{
  "name": "gitup-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "package": "pnpm -r run package",
    "lint": "pnpm -r run lint"
  },
  "workspaces": [
    "packages/*"
  ]
}
