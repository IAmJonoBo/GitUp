# Configuration

## Environment variables

- `GEMINI_API_KEY` - API key for server-side Gemini access. Stored in `.env` and never committed.

## Files

- [.env.example](../../.env.example) - Template for local environment values.

## VS Code settings

- `gitup.nodeVersion` - Preferred Node.js version for scaffold generation when `nvm current` is not available.
