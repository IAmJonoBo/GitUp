# Tools

Use these scripts for repeatable local and CI workflows.

## Scripts

- `verify.sh` - Runs format checks, lint, typecheck, tests, and builds.
- `release.sh` - Packages the VS Code extension into a VSIX.
